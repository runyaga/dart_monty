name: Publish dart_monty

on:
  push:
    tags:
      - 'v[0-9]*.[0-9]*.[0-9]*'

jobs:
  publish:
    uses: ./.github/workflows/_publish-dart-package.yaml
    permissions:
      id-token: write
    with:
      package-name: dart_monty
      tag-prefix: v
      uses-flutter: true
      has-tests: false
      analyze-scope: lib
