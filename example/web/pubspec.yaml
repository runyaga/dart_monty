name: web_example
description: Run Python from Dart in the browser via WASM.
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  dart_monty:
    path: ../..
  file: ^7.0.0
  web: ^1.1.1
