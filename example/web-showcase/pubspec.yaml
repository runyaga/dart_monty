name: web_showcase
description: Python in the browser — DOM, fetch, localStorage via Dart bridge.
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  web: ^1.1.1
dev_dependencies:
  lints: ^6.1.0
